import 'package:flutter/material.dart';

class NumbersCastScreen extends StatelessWidget {
  const NumbersCastScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("Numbers"),
    );
  }
}
