import 'package:flutter/material.dart';

class ResponseCastScreen extends StatelessWidget {
  const ResponseCastScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: const Center(
        child: Text("Response"),
      ),
    );
  }
}
